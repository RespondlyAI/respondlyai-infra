version: '3.8'

services:
  # --- DATABASES ---
  auth-db:
    image: postgres:17-alpine
    container_name: auth-db
    environment:
      POSTGRES_DB: auth_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    networks:
      - respondlyai-net

  org-db:
    image: postgres:17-alpine
    container_name: organization-db
    environment:
      POSTGRES_DB: org_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
    networks:
      - respondlyai-net

  auth-redis:
    image: redis:7-alpine
    container_name: auth-redis
    command: redis-server --requirepass password --appendonly yes
    ports:
      - "6379:6379"
    networks:
      - respondlyai-net

  # --- MICROSERVICES ---
  # auth-service:
  #   image: shubhojitmitradev/auth-service-backend-server:latest
  #   container_name: auth-service
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - DB_URL=jdbc:postgresql://auth-db:5432/auth_db
  #     - REDIS_HOST=auth-redis
  #     - THUNDERMAIL_TOKEN=tim_9cd9ac671e3ad8ebc04fb23053057c81
  #     - JWT_SECRET_KEY=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  #   depends_on:
  #     - auth-db
  #     - auth-redis
  #   networks:
  #     - respondlyai-net

  # organization-service:
  #   image: shubhojitmitradev/organization-service:latest
  #   container_name: organization-service
  #   ports:
  #     - "8081:8081"
  #   environment:
  #     - DB_URL=jdbc:postgresql://organization-db:5432/org_db
  #     - JWT_SECRET_KEY=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  #   depends_on:
  #     - org-db
  #   networks:
  #     - respondlyai-net

networks:
  respondlyai-net:
    driver: bridge
